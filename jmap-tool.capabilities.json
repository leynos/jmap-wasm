{
  "version": "0.1.0-beta5",
  "wit_version": "0.3.0",
  "capabilities": {
    "http": {
      "allowlist": [
        {
          "host": "api.fastmail.com",
          "path_prefix": "/",
          "methods": ["GET", "POST"]
        }
      ],
      "credentials": {
        "jmap_token": {
          "secret_name": "jmap_token",
          "location": {
            "type": "bearer"
          },
          "host_patterns": ["api.fastmail.com"]
        }
      },
      "rate_limit": {
        "requests_per_minute": 60,
        "requests_per_hour": 3600
      },
      "timeout_secs": 30
    },
    "secrets": {
      "allowed_names": ["jmap_token", "jmap_*"]
    }
  },
  "auth": {
    "secret_name": "jmap_token",
    "display_name": "JMAP",
    "instructions": "Use Configure to store a bearer token for Fastmail JMAP. If you target another provider, update both the HTTP allowlist and credential host patterns before installing the tool.",
    "env_var": "JMAP_TOKEN"
  },
  "setup": {
    "required_secrets": [
      {
        "name": "jmap_token",
        "prompt": "Enter the bearer token for your JMAP account"
      }
    ]
  }
}
